class Solution(object):
    def runningSum(self, nums):
        temp=[]
        temp.append(nums[0])
        for i in range(1,len(nums)):
            temp.append(int(temp[i-1]+nums[i]))
        return temp
